set(autostart ${PLUGIN_USB_MASS_STORAGE_AUTOSTART})

if(PLUGIN_USB_MASS_STORAGE_STARTUPORDER)
set (startuporder ${PLUGIN_USB_MASS_STORAGE_STARTUPORDER})
endif()

map()
   key(root)
   map()
       kv(mode ${PLUGIN_USB_MASS_STORAGE_MODE})
       kv(locator lib${PLUGIN_IMPLEMENTATION}.so)
   end()
end()
ans(configuration)

