set(autostart ${PLUGIN_USBDEVICE_AUTOSTART})

if(PLUGIN_USBDEVICE_STARTUPORDER)
set (startuporder ${PLUGIN_USBDEVICE_STARTUPORDER})
endif()

map()
   key(root)
   map()
       kv(mode ${PLUGIN_USBDEVICE_MODE})
       kv(locator lib${PLUGIN_IMPLEMENTATION}.so)
   end()
end()
ans(configuration)

