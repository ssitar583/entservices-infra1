set(autostart ${PLUGIN_ENTOSMIGRATIONTESTAPP_AUTOSTART})
set(preconditions Platform)

if(PLUGIN_ENTOSMIGRATIONTESTAPP_STARTUPORDER)
    set (startuporder ${PLUGIN_ENTOSMIGRATIONTESTAPP_STARTUPORDER})
endif()

map()
   key(root)
   map()
       kv(mode ${PLUGIN_ENTOSMIGRATIONTESTAPP_MODE})
       kv(locator lib${PLUGIN_IMPLEMENTATION}.so)
   end()
end()
ans(configuration)