set(autostart ${PLUGIN_MIGRATIONRESTORER_AUTOSTART})
set(preconditions Platform)

if(PLUGIN_MIGRATIONRESTORER_STARTUPORDER)
    set (startuporder ${PLUGIN_MIGRATIONRESTORER_STARTUPORDER})
endif()

map()
   key(root)
   map()
       kv(mode ${PLUGIN_MIGRATIONRESTORER_MODE})
       kv(locator lib${PLUGIN_IMPLEMENTATION}.so)
   end()
end()
ans(configuration)